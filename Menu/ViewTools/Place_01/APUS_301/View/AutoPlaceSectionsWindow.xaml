<Window x:Class="Revit26_Plugin.APUS_301.MVVM.AutoPlaceSectionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="APUS 301 – Auto Place Sections"
        Width="520"
        Height="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title block -->
            <RowDefinition Height="Auto"/>
            <!-- Filter -->
            <RowDefinition Height="*"/>
            <!-- Section list -->
            <RowDefinition Height="Auto"/>
            <!-- Selection buttons -->
            <RowDefinition Height="Auto"/>
            <!-- Status + Place -->
        </Grid.RowDefinitions>

        <!-- ========================= -->
        <!-- Title Block Selection     -->
        <!-- ========================= -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Title Block"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding TitleBlocks}"
                      SelectedItem="{Binding SelectedTitleBlock}"
                      DisplayMemberPath="Name"
                      Height="26"/>
        </StackPanel>

        <!-- ========================= -->
        <!-- Filter                    -->
        <!-- ========================= -->
        <TextBox Grid.Row="1"
                 Height="26"
                 Margin="0,0,0,8"
                 Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Filter section names"/>

        <!-- ========================= -->
        <!-- Sections List             -->
        <!-- ========================= -->
        <Border Grid.Row="2"
                BorderThickness="1"
                BorderBrush="#CCC"
                CornerRadius="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Sections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}"
                                      IsChecked="{Binding IsSelected}"
                                      Margin="6,3"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- ========================= -->
        <!-- Selection Buttons         -->
        <!-- ========================= -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    Margin="0,8,0,0">
            <Button Content="Select All"
                    Width="90"
                    Margin="0,0,6,0"
                    Command="{Binding SelectAllCommand}"/>

            <Button Content="Select None"
                    Width="90"
                    Margin="0,0,6,0"
                    Command="{Binding SelectNoneCommand}"/>

            <Button Content="Reverse"
                    Width="90"
                    Command="{Binding ReverseCommand}"/>
        </StackPanel>

        <!-- ========================= -->
        <!-- Status + Place Button     -->
        <!-- ========================= -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="{Binding StatusText}"
                       Foreground="Gray"/>

            <Button Grid.Column="1"
                    Content="Place Sections"
                    Width="130"
                    Height="32"
                    Command="{Binding PlaceCommand}"/>
        </Grid>

    </Grid>
</Window>
