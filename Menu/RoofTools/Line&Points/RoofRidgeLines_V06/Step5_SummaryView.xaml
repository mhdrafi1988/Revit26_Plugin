<UserControl x:Class="Revit26_Plugin.RoofRidgeLines_V06.Views.Steps.Step5_SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">

    <StackPanel Margin="20">

        <TextBlock Text="Summary"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,12"/>

        <StackPanel>

            <TextBlock Text="Detail Lines Created:" FontWeight="Bold"/>
            <TextBlock Text="{Binding DetailLinesCreated}"/>

            <TextBlock Text="Shape Points Added:"
                       FontWeight="Bold"
                       Margin="0,8,0,0"/>
            <TextBlock Text="{Binding ShapePointsAdded}"/>

            <TextBlock Text="Execution Time (seconds):"
                       FontWeight="Bold"
                       Margin="0,8,0,0"/>
            <TextBlock Text="{Binding ExecutionTimeSeconds, StringFormat=F2}"/>

        </StackPanel>

        <TextBlock Text="Warnings:"
                   FontWeight="Bold"
                   Margin="0,14,0,6"/>

        <ItemsControl ItemsSource="{Binding Warnings}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="•  {Binding}" Foreground="DarkOrange"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </StackPanel>
</UserControl>
