<Window x:Class="Revit26_Plugin.RRLPV4.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Roof Ridge Processor V4" Height="500" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#F5F5F5">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Status -->
        <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" FontSize="14" Margin="0,0,0,10"/>
        <!-- Controls -->
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="Detail Line Type:" FontWeight="SemiBold"/>
            <ComboBox ItemsSource="{Binding DetailTypes}" SelectedItem="{Binding SelectedDetailType}" Width="300" Height="30"/>
            <TextBlock Text="Division Strategy:" FontWeight="SemiBold" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding Divisions}" SelectedItem="{Binding SelectedDivision}" Width="300" Height="30"/>
            <Button Content="Execute" Command="{Binding StartCommand}" Width="120" Height="35" Margin="0,15,0,0" HorizontalAlignment="Left"/>
        </StackPanel>
        <!-- Live Log -->
        <Border Grid.Row="4" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" Padding="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListView ItemsSource="{Binding Logs}" Height="200">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
        </Border>
        <!-- Close -->
        <Button Grid.Row="5" Content="Close" Command="Close" Width="100" Height="30" HorizontalAlignment="Right"/>
    </Grid>
</Window>