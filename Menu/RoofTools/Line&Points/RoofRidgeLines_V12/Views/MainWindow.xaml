<Window x:Class="Revit26_Plugin.RoofTools.LineAndPoints.RoofRidgeLines_V12.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Roof Ridge Lines"
        Height="420"
        Width="420"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Point Interval (meters)" />
            <TextBox Margin="0,4,0,8"
                     Text="{Binding PointInterval}" />

            <Button Height="32"
                    Content="Select Roof"
                    Command="{Binding SelectRoofCommand}" />

            <TextBlock Margin="0,8,0,0"
                       Text="{Binding StatusMessage}"
                       FontWeight="Bold"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Live Log" Margin="0,12,0,0">
            <ListBox ItemsSource="{Binding Log}" />
        </GroupBox>
    </Grid>
</Window>
