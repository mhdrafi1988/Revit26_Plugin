<Window x:Class="Revit26_Plugin.RoofTools.LineAndPoints.RoofRidgeLines_V12.Views.SummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Operation Summary"
        Height="160"
        Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Success, StringFormat=Success: {0}}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <!-- Points -->
        <TextBlock Grid.Row="1"
                   Text="{Binding ShapePointsAdded, StringFormat=Shape Points Added: {0}}"
                   Margin="0,0,0,12"/>

        <!-- Close -->
        <Button Grid.Row="2"
                Content="Close"
                Width="80"
                Height="26"
                HorizontalAlignment="Right"
                Click="OnCloseClicked"/>
    </Grid>
</Window>
