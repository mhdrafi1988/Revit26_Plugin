<Window x:Class="Revit26_Plugin.RoofTools.LineAndPoints.RoofRidgeLines_V11.Views.SummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing Summary"
        Height="260"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Operation Summary"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="10,0">
            <TextBlock Text="{Binding Success, StringFormat=Success: {0}}"
                       Margin="0,0,0,6"/>

            <TextBlock Text="{Binding Points, StringFormat=Shape Points Added: {0}}"
                       Margin="0,0,0,6"/>

            <TextBlock Text="{Binding Duration, StringFormat=Duration: {0}}"/>
        </StackPanel>

        <!-- Close -->
        <Button Grid.Row="2"
                Content="Close"
                Width="90"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Click="OnCloseClicked"/>
    </Grid>
</Window>
