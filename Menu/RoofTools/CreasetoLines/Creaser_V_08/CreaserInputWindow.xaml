<Window x:Class="Revit26_Plugin.Creaser_V08.Commands.UI.CreaserInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Creaser Input"
        Height="180"
        Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Drain cluster radius (mm)" />
            <TextBox x:Name="RadiusBox" Text="1000" Margin="0,2,0,8"/>

            <TextBlock Text="Tolerance (mm)" />
            <TextBox x:Name="ToleranceBox" Text="5" Margin="0,2,0,8"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="5" Content="OK" Click="OnOk"/>
            <Button Width="80" Margin="5" Content="Cancel" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
