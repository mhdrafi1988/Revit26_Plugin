<Window x:Class="Revit26_Plugin.Creaser_V03_03.Views.CreaserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Creaser"
        Height="480"
        Width="360">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0"
                  ItemsSource="{Binding LineBasedFamilies}"
                  SelectedItem="{Binding SelectedFamily}"
                  DisplayMemberPath="Name"
                  Margin="8"/>

        <Button Grid.Row="1"
                Content="Run"
                Command="{Binding RunCommand}"
                Margin="8"
                Height="32"/>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding LogMessages}"
                 Margin="8"/>
    </Grid>
</Window>
