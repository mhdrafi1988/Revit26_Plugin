<Window
    x:Class="Revit26_Plugin.Creaser_adv_V001.Views.CreaserAdvWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Creaser Advanced"
    Height="500"
    Width="520"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Creaser Advanced"
            FontSize="20"
            FontWeight="Bold"
            Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock
                Text="{Binding RoofInfo}"
                Foreground="Gray"
                Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Detail Item:" Width="100"/>
                <ComboBox
                    Width="300"
                    ItemsSource="{Binding DetailItemSymbols}"
                    SelectedItem="{Binding SelectedDetailItem}"
                    DisplayMemberPath="Name"/>
            </StackPanel>

            <Button
                Content="Run Creaser"
                Height="36"
                FontWeight="Bold"
                Command="{Binding RunCommand}"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Log">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding Log}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                Text="{Binding}"
                                FontFamily="Consolas"
                                FontSize="12"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>