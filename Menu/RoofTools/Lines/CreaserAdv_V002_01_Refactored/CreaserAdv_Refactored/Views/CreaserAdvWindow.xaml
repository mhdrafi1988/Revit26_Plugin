<Window
    x:Class="Revit26_Plugin.CreaserAdv_V003_01.Views.CreaserAdvWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Revit26 – Creaser Advanced"
    Width="520"
    Height="520"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/Revit26_Plugin;component/Shared/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Detail Item selector -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock
                Text="Detail Item"
                Style="{StaticResource LabelTextBlockStyle}"
                Margin="0,0,0,4"/>

            <ComboBox
                ItemsSource="{Binding DetailSymbols}"
                SelectedItem="{Binding SelectedDetailSymbol}"
                DisplayMemberPath="Name"
                Height="26"/>
        </StackPanel>

        <!-- Action buttons -->
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Left"
            Margin="0,0,0,10">

            <Button
                Content="Run"
                Width="90"
                Margin="0,0,10,0"
                Style="{StaticResource PrimaryButtonStyle}"
                Command="{Binding RunCommand}"/>

            <Button
                Content="Clear Log"
                Width="90"
                Style="{StaticResource SecondaryButtonStyle}"
                Command="{Binding ClearLogCommand}"/>
        </StackPanel>

        <!-- Log panel -->
        <GroupBox
            Grid.Row="2"
            Header="Log">

            <ListBox
                ItemsSource="{Binding LogEntries}"
                FontFamily="Consolas"
                FontSize="11"
                BorderThickness="0"
                ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </GroupBox>
    </Grid>
</Window>
