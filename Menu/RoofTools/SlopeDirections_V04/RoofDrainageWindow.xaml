<Window x:Class="Revit_26.CornertoDrainArrow_V05.RoofDrainageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Roof Drainage – Line Detail"
        Height="450"
        Width="700">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Family Selection -->
        <GroupBox Header="Line-Based Detail Item" Grid.Row="0" Margin="0,0,0,10">
            <ComboBox ItemsSource="{Binding DetailFamilies}"
                      SelectedItem="{Binding SelectedFamily}"
                      DisplayMemberPath="DisplayName"
                      Height="25"/>
        </GroupBox>

        <!-- Buttons -->
        <Button Grid.Row="1"
                Content="Place Test Line Detail"
                Width="200"
                Height="30"
                Command="{Binding PlaceTestDetailCommand}"/>

        <!-- Log -->
        <GroupBox Grid.Row="2" Header="Log" Margin="0,10,0,0">
            <ListBox ItemsSource="{Binding LogEntries}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Message}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
