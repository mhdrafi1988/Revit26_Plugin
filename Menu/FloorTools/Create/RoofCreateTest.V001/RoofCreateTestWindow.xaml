<Window x:Class="Revit26_Plugin.RoofCreateTest.V001.UI.RoofCreateTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:Revit26_Plugin.Shared.Models"
        Title="RoofCreateTest — V001"
        Width="620" Height="620"
        MinWidth="620" MinHeight="620"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner"
        Background="LightBlue">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Revit26_Plugin;component/Shared/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters instantiated locally, after merged dictionaries -->
            <conv:LogLevelToColorConverter x:Key="LogLevelToColorConverter"/>
            <conv:InverseBoolConverter     x:Key="InverseBoolConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource HeaderBar}" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Style="{StaticResource HeaderTitle}" Text="RoofCreateTest — V001"/>
                <TextBlock Style="{StaticResource HeaderSubtitle}"
                           Text="NewFootPrintRoof isolation test · ExternalEvent context"/>
            </StackPanel>
        </Border>

        <!-- Parameters card -->
        <Border Grid.Row="1" Style="{StaticResource SectionCard}">
            <StackPanel Margin="0,0,0,10">
                <TextBlock Style="{StaticResource SectionTitle}" Text="Roof parameters"/>

                <StackPanel Style="{StaticResource FieldRow}">
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Level"/>
                    <ComboBox Style="{StaticResource StandardComboBox}"
                              ItemsSource="{Binding Levels}"
                              SelectedItem="{Binding SelectedLevel}"
                              DisplayMemberPath="Name"
                              IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
                </StackPanel>

                <StackPanel Style="{StaticResource FieldRow}">
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Roof type"/>
                    <ComboBox Style="{StaticResource StandardComboBox}"
                              ItemsSource="{Binding RoofTypes}"
                              SelectedItem="{Binding SelectedRoofType}"
                              DisplayMemberPath="Name"
                              IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
                </StackPanel>

                <TextBlock Style="{StaticResource HintText}" Margin="12,4,12,0"
                           Text="Footprint: 10 m × 10 m rectangle, centered at (0, 0). Fixed — no inputs."/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Style="{StaticResource PrimaryButton}" Content="Create roof (legacy API)"
                    Command="{Binding CreateRoofCommand}" Margin="0,0,8,0"/>
            <Button Style="{StaticResource AccentButton}" Content="Create roof (new API)"
                    Command="{Binding CreateRoofV2Command}" Margin="0,0,8,0"/>
            <Button Style="{StaticResource OutlineButton}" Content="Close"
                    Height="38" Click="OnCloseClick"/>
        </StackPanel>

        <!-- Log panel -->
        <Border Grid.Row="3" Background="{StaticResource BrushLogBg}" CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource BrushLogTitleBar}" CornerRadius="10,10,0,0" Padding="10,5">
                    <Grid>
                        <TextBlock Text="Log" Foreground="{StaticResource BrushLogText}"
                                   FontSize="11.5" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource DarkGhostButton}" Content="Copy all"
                                    Command="{Binding CopyAllCommand}" Margin="0,0,6,0"/>
                            <Button Style="{StaticResource DarkGhostButton}" Content="Copy selected"
                                    Click="OnCopySelectedClick" Margin="0,0,6,0"/>
                            <Button Style="{StaticResource DarkGhostButton}" Content="Clear"
                                    Command="{Binding ClearLogCommand}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <ListBox x:Name="LogListBox" Grid.Row="1"
                         ItemsSource="{Binding LogEntries}"
                         SelectionMode="Extended"
                         Background="Transparent"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="11"
                         Padding="6,4"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="{Binding Level, Converter={StaticResource LogLevelToColorConverter}}">
                                <Run Text="{Binding Time, Mode=OneWay, StringFormat=HH:mm:ss}"/>
                                <Run Text="  "/>
                                <Run Text="{Binding Level, Mode=OneWay}"/>
                                <Run Text="  "/>
                                <Run Text="{Binding Message, Mode=OneWay}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="4,1"/>
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>
