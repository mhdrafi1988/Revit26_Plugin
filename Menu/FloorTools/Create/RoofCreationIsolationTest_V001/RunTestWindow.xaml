<Window x:Class="Revit26_Plugin.RoofCreationIsolationTest.V001.UI.Views.RunTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:converters="clr-namespace:Revit26_Plugin.Shared.Models"
        mc:Ignorable="d"
        Title="Roof Creation Isolation Test"
        Width="720" Height="520"
        MinWidth="560" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Background="#1E3A5F">

    <Window.Resources>
        <ResourceDictionary>

            <!-- Local converter instance — per Converters.cs convention -->
            <converters:LogLevelToColorConverter x:Key="LogLevelToColorConverter"/>

            <!-- Local navy-theme brushes (self-contained, no external SharedStyles dependency) -->
            <SolidColorBrush x:Key="HeaderTextBrush" Color="#EAF1F8"/>
            <SolidColorBrush x:Key="SubtleTextBrush" Color="#9FB3C8"/>
            <SolidColorBrush x:Key="BorderBrush" Color="#3A5A80"/>
            <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#16293F"/>
            <SolidColorBrush x:Key="PrimaryButtonBackground" Color="#2E5A8F"/>
            <SolidColorBrush x:Key="PrimaryButtonBackgroundDisabled" Color="#2A3E52"/>
            <SolidColorBrush x:Key="SecondaryButtonBackground" Color="#24405E"/>

            <!-- Local button styles -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource PrimaryButtonBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{StaticResource PrimaryButtonBackgroundDisabled}"/>
                                    <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SecondaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource SecondaryButtonBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Header -->
            <RowDefinition Height="Auto"/>   <!-- Run row -->
            <RowDefinition Height="Auto"/>   <!-- Log toolbar -->
            <RowDefinition Height="*"/>      <!-- Log list -->
            <RowDefinition Height="Auto"/>   <!-- Summary -->
            <RowDefinition Height="Auto"/>   <!-- Commit buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Roof Creation Isolation Test"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource HeaderTextBrush}"/>
            <TextBlock Text="V001 — Diagnostic tool for NewFootPrintRoof() failures"
                       FontSize="12"
                       Foreground="{StaticResource SubtleTextBrush}"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Run row -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Run Test"
                    Command="{Binding RunCommand}"
                    IsEnabled="{Binding IsRunEnabled}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="140" Height="32"/>
        </StackPanel>

        <!-- Log toolbar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Execution Log"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource HeaderTextBrush}"/>
            <Button Content="Copy All"
                    Command="{Binding CopyAllCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="16,0,0,0" Width="90" Height="26"/>
            <Button Content="Copy Selected"
                    Command="{Binding CopySelectedCommand}"
                    CommandParameter="{Binding ElementName=LogListBox, Path=SelectedItems}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="8,0,0,0" Width="110" Height="26"/>
            <Button Content="Export Logs (.txt)"
                    Command="{Binding ExportLogCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="8,0,0,0" Width="130" Height="26"/>
            <Button Content="Clear"
                    Command="{Binding ClearLogCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="8,0,0,0" Width="70" Height="26"/>
        </StackPanel>

        <!-- Log list -->
        <Border Grid.Row="3"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Background="{StaticResource PanelBackgroundBrush}">
            <ListBox x:Name="LogListBox"
                     ItemsSource="{Binding LogEntries}"
                     SelectionMode="Extended"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     Background="Transparent"
                     BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   TextWrapping="Wrap"
                                   Foreground="{Binding Level, Converter={StaticResource LogLevelToColorConverter}}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Summary -->
        <TextBlock Grid.Row="4"
                   Text="{Binding SummaryText}"
                   Margin="0,10,0,0"
                   FontWeight="Bold"
                   Foreground="{StaticResource HeaderTextBrush}"/>

        <!-- Commit buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Close"
                    Click="CloseButton_Click"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Width="90" Height="30"/>
        </StackPanel>
    </Grid>
</Window>
